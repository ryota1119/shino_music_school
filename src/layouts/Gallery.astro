---
import GallerySlide from "@components/GallerySlide.astro";
import GalleryWrapper from "./GalleryWrapper.astro";

interface Props {
    bgColor: string,
}

const bgColor = Astro.props.bgColor;
---

<GalleryWrapper collor={bgColor} title="Gallery">
    <GallerySlide/>
</GalleryWrapper>